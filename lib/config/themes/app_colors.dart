import 'package:flutter/material.dart' show Color;

/// App's colour palette
abstract class AppColors {
  static const primary = Color(0xFF1A1810);
  static const primaryLight = Color(0xFFCEC9AE);

  static const background = Color(0xFF1A1C1E);
  static const surface = Color(0xFF242729);
  static const text = Color(0xFFFFFFFF);

  static const lightBackground = Color(0xFFF7F9FC);
  static const lightSurface = Color(0xFFEFF2F5);
  static const lightText = Color(0xFF222A36);
}
